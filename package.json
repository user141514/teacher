{
  "name": "management-coach-agent-prototype",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --env-file=.env server/index.js",
    "start": "node server/index.js",
    "test:server": "node --test tests/server.*.test.js tests/start-script.test.js",
    "test:e2e": "playwright test",
    "test": "npm run test:server && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "express": "^5.2.1",
    "markdown-it": "14.3.0"
  }
}
